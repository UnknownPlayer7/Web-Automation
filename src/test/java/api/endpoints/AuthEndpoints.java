package api.endpoints;

import lombok.experimental.UtilityClass;

@UtilityClass
public class AuthEndpoints {
    public final String BASIC_AUTH = "/basic_auth";
    public final String DIGEST_AUTH = "/digest_auth";
}
